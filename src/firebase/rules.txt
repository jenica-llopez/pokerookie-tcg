// ============================================================
// FIRESTORE SECURITY RULES
// Copy and paste these into:
// Firebase Console → Firestore Database → Rules → Edit rules
// ============================================================
//
// After getting your secretary's UID, replace
// REPLACE_WITH_SECRETARY_UID below, then re-publish.
//
// ============================================================

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return request.auth != null && (
        request.auth.uid == "juRb7XSMlaWCRni9XyA7uXec2nr1" ||   // Jenica
        request.auth.uid == "REPLACE_WITH_SECRETARY_UID"          // Secretary
      );
    }

    match /items/{itemId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /shipments/{shipmentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }
  }
}
